import React from "react"
import { Text, View } from "react-native"

const VideosSaved: React.FC = () => {
  return (
    <View className="h-full bg-background">
      <Text className="text-4xl text-white">Videos Saved</Text>
    </View>
  )
}

export default VideosSaved
